# 歌词模块完整点击区域修复

- 基线：`b51c7fbcddaa64bfa65bc2271079eda35c7ca023`。
- 范围：App 歌词模块 XAML、桌面回归测试。
- 原因：歌词模块背景为空，空白处命中外层 Grid，无法沿事件源父级识别为 Lyrics 模块。
- 修复：在 LyricsModuleView 根控件设置透明背景，让完整模块矩形参与命中。继续使用现有操控键判断与模块归属路由。
- 验证：Release 构建退出码 0（190 项既有警告）；桌面套件 220 PASS、0 FAIL（跳过独立版本变更 fixture，本次未更改版本逻辑）。
- 新回归：WPF 布局后使用 VisualTreeHelper.HitTest，检查三种宽度、空/短/长歌词下的九个坐标，共 81 个歌词区域命中点；同时检查相邻播放控制区域不属于歌词。视觉树测试不要求显示窗口，不等同于实机点击验收。
- 反向验证：移除透明背景后，同一回归在 240 宽度的 (0.5, 0.5) 处失败，命中 Grid；恢复修复后全部通过。
- 实机验收：默认点击穿透；按住操控键点击歌词模块两侧及上下空白应刷新；按住操控键点击相邻播放控制仍只执行播放操作。
- 候选策略：继续生成本地 `3.2.36-Beta`，沿用当前轮修复版本；旧候选由发布脚本归档。实际操作验收需单独确认。
