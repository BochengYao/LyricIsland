# 发布交接：v3.2.38 Beta

- 日期：2026-09-09。
- 发布线程：Release / GitHub。
- 基线提交：`ebc0932b573d4421c4a4e5dac9a3b26ab5bc3f66`。
- 版本：`3.2.38-Beta`；程序集文件版本 `3.2.38.0`。
- 目标：修复 v3.2.37 的歌词翻译、逐字追踪与歌词坞回归，并生成本地框架依赖 `win-x64` 候选与 GitHub 代码推送。

## 验收

- Astra 轻度首轮终验发现“密集短句缺译会复用同一译文”和“歌词坞从非安全空间恢复后不自动重显”两项阻断；Terra 中度补修后，Astra 第二轮终验通过。
- 独立 Release 构建 0 warning、0 error；桌面回归 247 PASS / 0 FAIL；Core 翻译契约通过；`git diff --check` 通过。
- 公开歌词源只读探针对比：v3.2.37 对 `You (Explicit)` 的 QQ 逐字+翻译包最终身份校验失败；v3.2.38 的应用完整歌词链对 `Roar` 与 `You` 均返回 word=true、translation=true、accepted=true。探针不写用户缓存，也不输出歌词正文。
- 歌词坞回归覆盖现场同构状态：`TaskbarDa=Absent`、恢复记录 `Enabled`、系统写入被拒绝时，安全空位仍显示；恢复期间禁止新 lease、Settings 自动化和恢复记录覆盖；unsafe→safe 环境变化会自动重显。

## 候选产物

- `publish/current/LyricHover.App.dll` 文件版本：`3.2.38.0`。
- SHA-256：`B0701624E38C4DD4CE37A0C85C6DFF8EC53F7B86A0DE413AA87CF74B500609EC`。
- 候选共 8 个文件，合计 24,551,955 bytes。

## 外部状态与限制

- GitHub 仅推送代码提交；不创建 GitHub Release、Tag 或附件。
- 未上传、验证、提交审核或发布 Microsoft Store 包，未部署官网。
- 自动化与公开接口探针不能替代用户机器上的真实播放器、DPI 和 Windows Widgets 完整现场验收。

## 回滚

- 代码回滚到基线 `ebc0932b573d4421c4a4e5dac9a3b26ab5bc3f66`；本地候选回滚使用 `publish/archive` 中归档的 v3.2.37 目录。
