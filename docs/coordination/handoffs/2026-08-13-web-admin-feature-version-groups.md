# 任务交接：后台新功能按版本分组编辑

- 日期：2026-08-13
- 任务线程：官网管理后台、API、鉴权和数据边界（`02-web-admin-data`）
- 基线提交：`574834dadbc68b43b83abe2e118655510ef20b45`
- 结果提交：当前实现提交后填写
- 允许修改范围：`website/components/AdminIncentives.tsx`、`docs/api/WEBSITE_API.md`、ESA/API 测试与本交接记录；未修改公开前台视觉、ESA 发布环境、桌面或资产。

## 已完成

- 后台“新功能内容”新增顶部“版本号”下拉框；候选来自已有功能条目、已发布/可编辑版本预告，并保留 `早期更新` 兼容入口。
- 选定版本后只展示该版本条目；黄色序号在当前版本组内从 `01` 重新开始。
- 新增条目自动继承当前版本；上移/下移只交换同版本条目，不能跨版本；显示/隐藏、编辑、删除也只作用于当前版本组。
- `早期更新` 仅作为旧记录兼容入口，新条目不能以其或非规范版本创建；需新增版本时先走版本预告入口。
- 翻译按钮仅处理当前选定版本的条目；四语字段和现有 `release_version` 持久化/API 契约保持不变。
- `WEBSITE_API.md` 已补充后台分组操作约束；API 测试覆盖多版本保存与 `major_version` 派生，公开 `/api/features` 响应结构未改。

## 给 05-quality-integration 的重点复核

- 选择两个以上版本，确认切换后只显示对应条目且每组序号从 `01` 开始。
- 在某版本新增条目，确认自动继承当前版本；移动条目时确认不能跨版本重排。
- 验证历史 `早期更新` 条目仍可读取、编辑和保存；新增条目不会落入该兼容版本。
- 分别编辑/翻译简中、英文、繁中、日文，确认保存后四语字段不串组、不丢失。
- 调用公开 `/api/features`，确认 `release_version` 原样返回、`major_version` 由服务端派生，前台不需要自行推断版本。

## 未修改 / 非目标

- 未修改 `/api/features` 的响应结构、数据库列、公开页面布局/视觉或 ESA 生产部署。
- 未触碰桌面端、资产、密钥、环境变量或真实用户数据。

## 验证

- `cd website && npm run typecheck`：通过。
- `cd website && npm run build:esa`：通过。
- `cd website && npm run test:esa-api`：通过；覆盖历史回退、缺版本拒绝、V2/V3 多版本保存和派生字段。
- `git diff --check`：通过。

## 风险与后续

- 版本预告历史上可能存在非 `vX.Y.Z` 的旧版本字符串；这些仍可作为候选读取，但新增条目会提示先创建规范完整版本。
- 发布前由质量线程独立复核后台交互与公开 API；发布线程仍需按 `RELEASE_CHECKLIST.md` 验证实际生产路由。
- 回滚点：`574834dadbc68b43b83abe2e118655510ef20b45`；回滚不删除已有功能 JSON 或历史版本记录。
