# 发布交接：v3.2.39 Beta 专辑封面缩放质量

- 日期：2026-09-09。
- 发布线程：Release / Local Candidate。
- 功能基线提交：`5a2a4f60404b0aeec4e12959315670f64e49ebf0`。
- 工作分支：`codex/feature/desktop-album-art-quality`。
- 结果提交：`4bbecedb86bf47e24052fb116abae1a2adfb7810`。
- 版本：`3.2.39-Beta`；程序集文件版本 `3.2.39.0`。
- 目标：把已通过 Sol 初检与 Astra 最终核验的专辑封面高质量缩放修复生成到根目录 `publish/current`。

## 变更与边界

- `ArtworkImage` 使用 WPF `HighQuality` 缩放；保留 42 DIP、`UniformToFill`、圆角裁切、原始字节解码和现有缓存语义。
- 发布脚本只递增 `Directory.Build.props` 补丁版本；未修改 Core 行为、用户设置、Store 身份或官网。
- 根工作树原有文档 WIP 未纳入本分支或提交。

## 生成与验证

- 首次 `publish.ps1 -NoLaunch`：桌面测试 247 PASS / 0 FAIL，随后因隔离 worktree 缺少 `win-x64` restore 资产而以 `NETSDK1047` 阻断；脚本已回滚版本并清理 staging，未替换候选。
- 执行 `dotnet restore LyricHover.App\LyricHover.App.csproj --runtime win-x64` 成功后，重新执行 `publish.ps1 -NoLaunch` 成功。
- 发布脚本完整测试：247 PASS / 0 FAIL。
- `win-x64` Release build：0 warning / 0 error；`dotnet publish` 成功。
- 候选未自动启动；没有把本地生成等同于真实播放器或多 DPI 视觉验收。

## 当前候选

- 路径：`D:\AppleMusicDesktopLyrics\publish\current`。
- 文件：递归 11 个文件，共 33,806,170 bytes。
- `LyricHover.App.dll` ProductVersion：`3.2.39-Beta`；FileVersion：`3.2.39.0`。
- `LyricHover.App.dll` SHA-256：`C71B5FC61742DF677627CD0795E4391403168EB453E4B13D3DBFC3EFFF7F494C`。
- 原 `3.2.38-Beta` 已可恢复地归档到 `D:\AppleMusicDesktopLyrics\publish\archive\v3.2.38-Beta`；归档 DLL SHA-256 仍为 `B0701624E38C4DD4CE37A0C85C6DFF8EC53F7B86A0DE413AA87CF74B500609EC`。
- `publish/README.md` 已更新为 `v3.2.39 Beta`。该目录由 `.gitignore` 排除，不进入代码提交。

## 外部状态与限制

- 功能提交与本交接的版本提交均已推送到 GitHub 分支 `codex/feature/desktop-album-art-quality`；远端 SHA 已核对为 `4bbecedb86bf47e24052fb116abae1a2adfb7810`。
- 未创建 GitHub Release、Tag 或附件。
- 未生成或上传 Microsoft Store 包，未提交审核或发布 Store，未部署官网。
- 真实播放器源图及 100%、150%、200% DPI 下的同源视觉 A/B 仍需用户机器验收；高质量重采样不能补回上游原图本身缺失的细节。

## 接收线程与行动

| 接收线程 | Evidence / Impact | Suggested Owner / 后续 |
| --- | --- | --- |
| Desktop UI & Interaction | UI 修复已通过自动化与独立审查，但缺少真实播放器和多 DPI 同源 A/B | 复核封面边缘、细字与纹理；记录源图像素尺寸、播放器和 DPI |
| Quality & Release | `publish/current` 已替换为上述 v3.2.39 指纹，旧 v3.2.38 已归档 | 以提交与 DLL SHA-256 复核候选；不要将本地候选表述为 Store/GitHub Release |
| Desktop Core | 未修改媒体会话或缓存；存在同曲首次字节持续复用的未证实风险 | 仅 Knowledge Sync；取得真实到达顺序证据后再决定是否立独立缓存任务 |
| 复盘测试当前版本 | 当前可执行候选已从 v3.2.38 变为 v3.2.39 | 后续复盘使用新版本和 DLL 指纹，保留自动化与实机视觉证据层级 |

### 实际发送回执（2026-09-09）

交接文档提交 `673f80626cf53034cecb0c170b8eb433701e8ef4` 已推送 GitHub。随后通过 Codex 原生任务消息发送完整文件路径、提交、候选指纹、证据边界和各自复核重点；四次调用均成功返回对应 `threadId`。发送成功只证明交接已到达，不代表接收方已经完成复核。

| 接收任务（应用当前名称） | threadId | 交付状态 |
| --- | --- | --- |
| Desktop UI & Interaction | `01a02e9b-fac1-7162-9d2e-5fb4e34d69cd` | 已发送；真实播放器与多 DPI A/B 待接收方复核 |
| Quality & Release | `01a02eb1-7236-7ca1-ae3a-f5ea37e82d01` | 已发送；候选与发布边界待接收方复核 |
| Desktop Core | `01a02e9b-01a5-7452-8245-98c80bbf9497` | 已发送 Knowledge Sync；无证据前不直接修改缓存 |
| 复盘测试当前版本 | `01a0748d-4e64-7e90-be63-c76864169f84` | 已发送；后续复盘使用 v3.2.39 与 DLL 指纹 |

## 回滚

- 本地产物回滚：将 `publish/current` 移出后，把 `publish/archive/v3.2.38-Beta` 恢复为 `publish/current`。
- 代码回滚：回到功能提交的父提交 `82e921a85182579367bf36ddf25fd2d2b10a9f72`；仅回退版本记录时使用本次版本提交的父提交。
