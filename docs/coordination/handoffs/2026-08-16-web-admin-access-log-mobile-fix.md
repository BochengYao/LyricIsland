# HANDOFF：访问日志移动端布局修复

- 日期：2026-08-16
- 基线：`3bd2648ea2ce931075565ce46879ee9de9363600`
- 初始审计提交：`7c60a01`
- 工作树：`.worktrees/web-admin-access-log-mobile-3bd2648`
- 范围：仅后台访问日志页面的移动显示；未修改 API、数据模型、鉴权、公开前台、桌面端或 ESA 发布环境。

## 修复内容

- 新增移动端访问日志语义卡片：时间/风险、前后台范围、事件、路径、方法/状态、详情、IP/地区/访客、设备/来源、语言/请求和长 User-Agent 均有明确字段。
- `max-width: 767px` 下隐藏 920px 宽表，显示卡片列表；桌面端继续使用原表格，不依赖缩放或横向滚动。
- 移动筛选区改为单列全宽输入/选择/清除按钮，最小高度 44px；长 IP、路径、请求 ID、来源和 User-Agent 使用可换行布局。
- 详情 JSON 保留折叠入口，并限制移动卡片内高度/宽度，避免长字段撑破视口。
- 未新增分页或改变日志接口；当前页面仍只提供既有本地筛选和显示数量，分页另需产品/API 任务。

## 验证

- `npm run typecheck`：通过。
- `npm run test:esa-api`：通过（`ESA API tests passed`）。
- `npm run build:esa`：通过；静态路由生成完成，`website/app/api` 已正常恢复。
- 初始截图位于 `docs/coordination/evidence/access-log-mobile-audit-2026-08-16/`，覆盖 320×568、360×800、390×844、414×896、844×390。

## 真实性边界与质量复核

- 当前环境没有可安全复用的管理员会话或非生产 UI 测试账号；本地 `/admin` 只读加载命中管理 GET 并返回 401，未发送登录、PATCH、数据写入或生产请求。
- 因此五视口截图是登录页证据，不是已登录访问日志数据截图；不得据此宣称生产日志面板已视觉验收。
- 请 `05-quality-integration` 在用户手动登录 `https://lyric-island.top/admin` 后，只读复核五个视口：长 IP/时间/路径/状态、筛选顺序、详情展开、键盘焦点、无数据态和长列表。
- 回滚点：`3bd2648`；本次无数据迁移、无接口契约变化、无 ESA 触发。
