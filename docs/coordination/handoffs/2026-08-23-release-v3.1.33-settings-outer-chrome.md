# 任务交接：v3.1.33 Beta 设置外圆角修复本地发布

- 日期：2026-08-23
- 任务线程：Desktop UI & Interaction / `codex/feature/desktop-settings-outer-chrome`
- UI 修复提交：`8636291`
- 版本提交：`f1ff268`
- 目标渠道：本地 `publish/current`；未上传 GitHub，未上传或提交 Microsoft Store

## 已完成

- 将版本源从 `3.1.32-Beta` 递增为 `3.1.33-Beta`。
- 将设置窗口、大卡片的 8px 圆角对齐及 DWM 外边框关闭纳入发布候选。
- 使用 `publish.ps1 -NoLaunch` 生成隔离 worktree 候选；脚本完成完整测试、win-x64 Release 构建和框架依赖发布。
- 将根目录旧 `publish/current` 精确归档为 `publish/archive/v3.1.32-Beta`，再把已验证候选提升到根 `publish/current`。
- 替换前结束了唯一从旧根 `publish/current` 运行的进程 PID 256808；3.1.33 未自动启动。
- 根 `publish/README.md` 已更新为 `v3.1.33 Beta`。

## 验证

- `dotnet restore LyricHover.sln`：退出码 0。
- `dotnet restore LyricHover.App/LyricHover.App.csproj --runtime win-x64`：退出码 0。
- `publish.ps1 -NoLaunch`：退出码 0；完整测试全部 PASS；win-x64 Release 构建 0 警告、0 错误。
- 根 `publish/current/LyricHover.App.dll`：FileVersion `3.1.33.0`，ProductVersion `3.1.33-Beta`。
- Release / 隔离候选 / 根 current DLL SHA-256：`049DDC9D33F54AD31104E76EE9857CB9D24068FBAD43C0BA2E8D7EDB1F8448D9`。
- 根 current 文件数：8；`deps.json` 与 `runtimeconfig.json` 前四字节均为 `123,13,10,32`，非 NUL。
- 归档 `v3.1.32-Beta` DLL SHA-256：`96A9911FABFFBCF2AE3AC2CF64DB46F4756E0E582C4D2DA89CC533EF89DBE358`。
- 提升后根 current 运行进程数：0。

## 状态边界

- 已构建：是。
- 已生成本地包：是，框架依赖版目录产物。
- 已提升并验证根 `publish/current`：是。
- 已上传 GitHub：否。
- 已上传 Microsoft Store：否。
- 已提交 Store 审核：否。
- 已发布到外部渠道：否。

## 回滚点

- 源码回滚点：`8636291`（版本递增前）。
- 产物回滚点：`D:\AppleMusicDesktopLyrics\publish\archive\v3.1.32-Beta`。
