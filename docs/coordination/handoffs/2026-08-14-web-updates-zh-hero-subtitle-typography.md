# 任务交接：简中新功能页核心副标题字重与间距

- 日期：2026-08-14
- 任务线程：01-web-frontend
- 基线提交：`origin/main` `f5da8e583e3ec106d18c737010e5d00d2a1a6631`
- 结果提交：待本分支提交
- 允许修改范围：公开更新页副标题的 CSS；不修改后台、API、鉴权、数据库、文案内容、根工作树或发布环境。

## 已完成

- `.updatesHeroSubtitle` 桌面端字重由 `600` 调整为常规 `400`，不再加粗。
- 桌面端字号从 `30px` 提升至 `38px`，与主标题的上间距从 `28px` 收紧至 `14px`。
- 窄屏字号从 `24px` 提升至 `30px`，上间距同样收紧为 `14px`。

## 未修改 / 非目标

- 未改英雄区文案、其他语言、版本选择、模块详情、更新亮点、导航、接口、后台、鉴权或数据。
- 未部署 ESA。

## 验证

- 命令：`npm run typecheck`
- 结果：通过。
- 命令：`npm run build:esa`
- 结果：通过；四语更新页静态路由生成成功。
- 本地 DOM/截图：通过本地 `next start` 和 Playwright/Edge 验证副标题计算样式为 `font-weight: 400`、`font-size: 38px`、`margin-top: 14px`；截图已人工核对。
- 未运行：`npm run test:esa-api`，因为本次仅改 CSS，未变更接口或读取契约。

## 风险与后续

- 已知限制：本地视觉验收使用模拟空内容，仅验证英雄区样式；发布后由发布线程确认 ESA 真实路由。
- 交接目标：项目统筹 / 发布线程。
- 回滚点：`f5da8e583e3ec106d18c737010e5d00d2a1a6631`。
