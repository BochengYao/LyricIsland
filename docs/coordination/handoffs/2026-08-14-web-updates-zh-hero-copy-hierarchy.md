# 任务交接：简中新功能页英雄区文案层级

- 日期：2026-08-14
- 任务线程：01-web-frontend
- 基线提交：`origin/main` `2097a40e05ecc2d759e224aaa6588dc6a8667e98`
- 结果提交：待本分支提交
- 允许修改范围：简中公开更新页的英雄区文案与前端展示样式；不修改后台、API、鉴权、数据库、内容数据、根工作树或发布环境。

## 已完成

- 简中英雄区顶部标签改为“更新”。
- 主标题改为“Lyric Hover的更新”。
- 增加独立核心副标题：“新功能与改进，一目了然。”
- 辅助说明改为“选择一个版本，了解本次更新的全部内容，包括功能升级、体验优化与问题修复。”
- 新增副标题仅在简中更新页显示；繁中、英文、日文的原有英雄区文案和结构保持不变。

## 未修改 / 非目标

- 未改更新亮点、版本选择、模块详情、导航、下载收尾、页脚、SEO 元数据或任何公开接口读取方式。
- 未改 `website/app/api/`、管理后台、鉴权、数据库或发布环境；未部署 ESA。

## 验证

- 命令：`npm run typecheck`
- 结果：通过。
- 命令：`npm run build:esa`
- 结果：通过；生成 16 条静态路由，包含四语更新页。
- 本地 DOM/截图：以本地 `next start` 和 Playwright/Edge 访问 `/updates`，断言四层简中文案完全匹配；访问 `/en/updates`，断言未生成 `.updatesHeroSubtitle` 且原标题保持。截图仅作本地验收，不提交。
- 未运行：`npm run test:esa-api`，因为未修改 API 或读取契约。

## 风险与后续

- 已知限制：本地截图使用模拟的空公开内容响应，只用于验证英雄区文案；发布后应由发布线程确认 ESA 真实路由。
- 交接目标：项目统筹 / 发布线程。
- 回滚点：`2097a40e05ecc2d759e224aaa6588dc6a8667e98`。
