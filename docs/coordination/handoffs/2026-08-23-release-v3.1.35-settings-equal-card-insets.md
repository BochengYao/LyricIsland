# 任务交接：v3.1.35 Beta 设置模块安全距离本地发布

- 日期：2026-08-23
- 任务线程：Desktop UI & Interaction / `codex/feature/settings-equal-card-insets`
- UI 修复提交：`6d09faf`
- 版本提交：`1f79643`
- 目标渠道：本地 `publish/current`；未上传 GitHub，未上传或提交 Microsoft Store

## 已完成

- 将版本源从 `3.1.34-Beta` 递增为 `3.1.35-Beta`。
- 将所有设置模块统一四边 24px 安全距离纳入发布候选。
- 使用 `publish.ps1 -NoLaunch` 生成隔离 worktree 候选；脚本完成完整测试、win-x64 Release 构建和框架依赖发布。
- 结束路径明确位于旧根 `publish/current` 的进程 PID 271556；将旧 `publish/current` 归档为 `publish/archive/v3.1.34-Beta`，再把已验证候选提升到根 `publish/current`。
- 根 `publish/README.md` 已更新为 `v3.1.35 Beta`；3.1.35 未自动启动。

## 验证

- `dotnet restore LyricHover.App/LyricHover.App.csproj --runtime win-x64`：退出码 0。
- `publish.ps1 -NoLaunch`：退出码 0；完整测试全部 PASS；win-x64 Release 构建 0 警告、0 错误。
- 根 `publish/current/LyricHover.App.dll`：ProductVersion `3.1.35-Beta`。
- 隔离候选 / 根 current DLL SHA-256：`8CC2BC58E7D06279808D7219A335ACC88317D537392B2390A4D500D22DDF864B`。
- 根 current 文件数：8；`deps.json` 与 `runtimeconfig.json` 前四字节均为 `123,13,10,32`，非 NUL。
- 归档 `v3.1.34-Beta` DLL SHA-256：`88E35657CC8FB8C7C0B1A7ECC7687C822D8151FF5141A11DF08C4D2241DCE418`。
- Windows 进程表仍列出路径不可读的旧残留 PID 38596；新版 current 未由发布脚本启动。

## 状态边界

- 已构建：是。
- 已生成本地包：是，框架依赖版目录产物。
- 已提升并验证根 `publish/current`：是。
- 已完成新实图验证：否，临时 WPF 预览工具受入口程序集资源装载限制。
- 已上传 GitHub：否。
- 已上传 Microsoft Store：否。
- 已提交 Store 审核：否。
- 已发布到外部渠道：否。

## 回滚点

- 源码回滚点：`6d09faf`（版本递增前）。
- 产物回滚点：`D:\AppleMusicDesktopLyrics\publish\archive\v3.1.34-Beta`。
