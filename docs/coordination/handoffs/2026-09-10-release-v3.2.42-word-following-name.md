# 任务交接：v3.2.42 Beta “逐字跟随”统一命名本地候选

- 日期：2026-09-10
- 任务线程：Desktop UI + Quality / Integration + Release
- 基线提交：`77cd50f`
- 结果提交：本交接所属提交
- 允许修改范围：逐字歌词用户可见命名、多语言映射、直接回归测试、版本与变更记录、本地 `publish/current`

## 已完成

- 歌词岛与歌词坞设置中的功能名称统一为“逐字跟随”。
- 四种界面语言同步为：简体中文“逐字跟随”、繁体中文“逐字跟隨”、英语“Word-by-word follow”、日语“一語ずつ追従”。
- 保留 `IslandWordTrackingEnabled`、`LyricDockWordTrackingEnabled` 等内部兼容标识，不改变设置键、默认值、序列化或功能行为。
- 面向用户的变更记录统一使用“逐字跟随”，并新增 `3.2.42` 条目。
- 通过仓库发布脚本将版本从 `3.2.41-Beta` 推进至 `3.2.42-Beta`；旧候选归档到 `publish/archive/v3.2.41-Beta/`，新候选位于 `publish/current/`。

## Changed Files

- `LyricHover.App/PlacementSettingsWindow.xaml`
- `LyricHover.App/UiLanguageService.cs`
- `LyricHover.Tests/Program.cs`
- `CHANGELOG.md`
- `Directory.Build.props`
- `publish/README.md`
- `publish/current/`（本地产物）
- 本交接文件

## 未修改 / 非目标

- 网站源代码中没有该功能的现有用户可见命名，因此没有制造新的官网文案或扩大网站发布范围。
- 未改写历史 Handoff、测试报告或历史证据中的旧称。
- 未修改 Core 逐字时序、歌词来源、缓存、翻译、播放器或 UI 时间线语义。
- 未改动工作树中既存的 `docs/desktop-core-domain-knowledge.md` 和托盘菜单交接 WIP。
- 未上传 GitHub、未创建 GitHub Release、未生成或上传 MSIX、未执行 Microsoft Store 上传或提交。

## 验证

- `git diff --check`：通过。
- 文案扫描：当前 App、网站与面向用户变更记录中不再存在“逐字追踪 / 逐字跟踪 / 逐字追蹤 / 逐字跟蹤”旧称；历史证据和测试否定断言除外。
- 首次发布尝试因 Windows SDK 探测权限失败；显式设置 SDK 路径后确认本机 NuGet 缓存缺少 Windows SDK Contracts，并执行 `dotnet restore LyricHover.App\LyricHover.App.csproj --runtime win-x64` 修复依赖图。
- `./publish.ps1 -NoLaunch`：252 项桌面回归全部通过；新增四语言命名回归通过；最终 `win-x64` Release build 为 0 warning、0 error；publish 成功。
- 产物：8 个文件，共 24,554,283 bytes；ProductVersion `3.2.42-Beta`，FileVersion `3.2.42.0`。
- SHA-256 `LyricHover.App.exe`：`5300CC4C6A67B1954B15545F8A6789970913E2F621D78AD1AB772CE9EBB9FA75`
- SHA-256 `LyricHover.App.dll`：`5A5945A2B6F54A77BB55DBF16CDA54709E1C3D2AD37A4D213CE0BD94C89CC368`
- SHA-256 `LyricHover.Core.dll`：`89C7B3EBB53D0BF05A5434E1BD88F01EFFDBF9C6DA667451DA87EB3C1CFF6EF0`

## 风险与后续

- 已知限制：尝试启动 `publish/current` 做可观察 UI 验收时，桌面控制授权超时，未完成设置页四语言截图或人工目视验证；自动化已直接验证四种语言映射和两个设置入口。
- 交接目标：本地 `publish/current` 候选；如需 GitHub、Store 或官网外部发布，需另行明确授权并独立记录渠道状态。
- 回滚点：基线提交 `77cd50f`；前一候选保留在 `publish/archive/v3.2.41-Beta/`。
