# 任务交接：官网激励计划导航改用 851 远星夜行手写体

- 日期：2026-08-10
- 任务线程：官网前台
- 基线提交：`d6992ca69f694fc19bf4b00b6a2898a88ef12386`
- 结果提交：`c23ef7d60abfabd085d68c14b12ce1d6d5feaf3b`
- 允许修改范围：公开官网导航字体、静态字体资源及各语言页面的字体预加载。

## 已完成

- 将简中“用户激励计划”、繁中“使用者激勵計畫”、英文“Community rewards”、日文“コミュニティ特典”统一改用 851 远星夜行手写体。
- 以 14 个按字形分片的 WOFF2 文件自托管，仅覆盖上述四个导航标签；全部使用 `font-display: swap`，不增加第三方运行时字体请求。
- 随字体资源保留作者许可文本 `851fontTerm.html`，并移除不再使用的小赖字体预加载。
- 保留既有 Mastercard 导航的胶囊几何、颜色、交互、菜单和非激励计划文本字体。

## 未修改 / 非目标

- 未修改管理后台、`website/app/api/`、数据库、鉴权、公开接口或 SEO 路由。
- 未发布生产、未触发 ESA 同步。

## 验证

- 命令：在 `website/` 运行 `npm run typecheck`、`npm run build:esa`、`npm run test:esa-api`。
- 结果：三项均通过（退出码 0）；ESA 构建导出 16 个静态路由。
- 静态输出：`/`、`/zh-hant/`、`/en/`、`/ja/` 分别包含正确的四种导航标签；`out/fonts/851-lakeus-night-writing/` 包含 14 个 WOFF2 分片（397,444 字节）及许可文本，生成 CSS 含 851 字体族和自托管路径。

## 风险与后续

- 已知限制：本次没有发布到 ESA，线上仍为此前字体；发布后应复核四个首页字体资源均为 200，并补充桌面和移动端视觉截图。
- 交接目标：网站发布 / ESA 线程。
- 回滚点：`d6992ca69f694fc19bf4b00b6a2898a88ef12386`。
