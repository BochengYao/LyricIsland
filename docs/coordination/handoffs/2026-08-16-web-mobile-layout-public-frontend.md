# 任务交接：官网公开前台移动端布局修复

- 日期：2026-08-16
- 任务线程：01-web-frontend（替补）
- 基线提交：`0c081881c847241393ad264c2b8e33d50f61dda9`
- 结果提交：本文件所在候选提交
- 允许修改范围：官网公开页面共享样式、任务独立 HANDOFF

## 已完成

- `<=767px` 隐藏首页完整互动演示模块，包括“亲手体验 / 动一动鼠标”标题和交互画布。
- `<=1023px` 将多播放器轨道切换为紧凑、可换行且不裁切的胶囊流；`>=1024px` 保留桌面轨道、动画和中心胶囊。
- 移动端建议卡片启用边界裁切和盒模型约束，点赞按钮保持卡片内、圆角胶囊且最小触控尺寸 `44px`。
- 移动端版本预告左右安全边距各 `24px`，序号列固定可收缩，标题与长连续文本允许安全断词。
- 共享组件与样式覆盖简中、英文、繁中、日文公开路由；未新增语言分支。

## 未修改 / 非目标

- 未修改后台、API、数据模型、鉴权、测试实现、共享接口文档或发布配置。
- 未读取、复制或修改 `D:\AppleMusicDesktopLyrics\.worktrees\web-mobile-layout-0c081881` 的未提交内容。
- 未修改根树、原暂停树、共享 npm cache 或 `package-lock.json`；未执行 ESA 部署。

## 验证

- 命令：`npm ci --cache .npm-cache-mobile-layout-01`
- 结果：通过，353 个包按锁文件安装；任务缓存随后精确删除，lockfile 无变化。npm 报告 8 个 high severity 依赖审计项，本任务未运行自动修复。
- 命令：`npm run typecheck`
- 结果：通过，退出码 0。
- 命令：`npm run build:esa`
- 结果：沙箱内首次因 Turbopack 无权读取 `C:\Users\14731\.config` 失败；获准在沙箱外以同一命令重跑通过，生成 16 个静态页面。
- 命令：`npm run test:esa-api`
- 结果：通过，输出 `ESA API tests passed`。
- 实际浏览器验收：系统 Edge headless 加载本次 `build:esa` 静态产物，滚动到对应区段后截图；动态建议和版本预告用浏览器路由注入仅用于布局验证的长文本样例，未写入应用或数据库。
- `320x568`：首页播放器胶囊分 4 行，全部位于视口内；互动演示 `display:none`；页面横向溢出 0px。
- `390x844`：建议卡片可见点赞按钮均位于卡片内且高度不小于 44px；页面横向溢出 0px。
- `844x390`：版本预告序号与长连续文本均位于视口内；页面横向溢出 0px。
- `1440x900`：互动演示显示，桌面播放器轨道保留；页面横向溢出 0px。
- 四语 `/`、`/en`、`/zh-hant`、`/ja` 在 `390x844` 下均确认互动演示隐藏且页面横向溢出 0px。
- 截图目录：`C:\Users\14731\.codex\visualizations\2026\08\16\01a009c2-16ab-7722-9c13-d766a95fcbc6\`（不进入提交）。

## 风险与后续

- 已知限制：浏览器截图使用本地构建产物和合成公开 API 响应，仅证明布局与断点；不代表生产 ESA 已发布或真实生产数据已复核。
- 交接目标：05-quality-integration 复核四语公开路由、指定视口截图及桌面断点；通过后交发布线程执行已授权 ESA 部署与生产路由验证。
- 回滚点：回退本文件所在候选提交即可恢复基线样式。
