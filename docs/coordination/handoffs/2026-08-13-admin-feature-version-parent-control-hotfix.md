# 任务交接：后台新功能版本父级控制紧急修复

- 日期：2026-08-13
- 任务线程：官网管理后台、API、鉴权和数据边界（`02-web-admin-data`）
- 基线提交：`88a825872c37ac76e08aa8e2328d3134f05fcd95`
- 结果提交：本交接所在的紧急修复提交（基于 `88a8258`）
- 允许修改范围：后台版本选择与条目操作、`featureVersionPanel` 窄范围后台样式、版本兼容辅助、接口文档、ESA API 测试和本交接记录；未修改根树 WIP、桌面、公开页面视觉或 ESA 发布环境。

## 已完成

- 将“版本号”从紧凑顶栏移到功能条目列表前的明确父级控制区，展示当前版本组和历史兼容提示。
- 默认优先选择可用规范版本；没有规范版本时明确显示“早期更新”历史兼容组和先创建版本预告的引导，不再把历史组伪装成当前发布版本。
- 版本候选继续来自既有条目、已发布/可编辑版本预告和 `早期更新`。预告短标签 `v3`、`v2.5` 在新增条目时转换并持久化为完整版本 `v3.0.0`、`v2.5.0`，与 `release_version` 唯一来源约定一致。
- 历史 `早期更新` 条目可浏览、编辑和保存，但不能在该组新增；新增条目只继承当前规范版本。
- 保持每个版本组内排序与 `01` 序号；保存校验显式允许历史兼容值，不要求为历史 9 条目编造或迁移版本号。
- 更新 `WEBSITE_API.md` 说明预告短标签规范化和兼容行为；ESA API 测试覆盖历史保存及 V2/V3/V2.5 版本输出。

## 未修改 / 非目标

- 未修改公开 `/api/features` 响应结构、Supabase schema 或任何数据记录；无数据库迁移。
- 未修改公开前台视觉、ESA 生产环境、桌面、资产、密钥、环境变量或真实用户数据。

## 验证

- `cd website && npm run typecheck`：通过。
- `cd website && npm run test:esa-api`：通过；覆盖历史兼容保存、缺版本拒绝和多版本派生。
- `cd website && npm run build:esa`：通过，生成四语静态路由。
- `git diff --check`：通过。

## 给质量线程的重点复核

- 进入后台时默认不落在历史兼容组（存在规范版本时）；无规范版本时历史组提示清晰可见。
- 选择版本预告 `v3`/`v2.5` 后新增条目，确认实际保存为 `v3.0.0`/`v2.5.0`，且条目只出现在当前组。
- 历史 `早期更新` 条目编辑四语字段并保存，确认不被阻塞、不被赋予虚构版本。
- 切换版本后仅显示该组条目，黄色序号从 `01` 开始，排序不跨组。
- 公开 API 保持 `release_version` 与服务端派生 `major_version`，前台无需自行推断。

## 风险与回滚

- 版本预告中的任意非数字短标签不会进入可新增候选；管理员需先创建规范数字版本。
- 未触发 ESA；发布线程需按 `RELEASE_CHECKLIST.md` 单独验证生产后台。
- 回滚点：`88a825872c37ac76e08aa8e2328d3134f05fcd95`；回滚不删除历史功能 JSON。
