# 任务交接：官网后台紧凑编辑器与版本预告删除

- 日期：2026-08-14
- 任务线程：官网管理后台 / API / 数据边界
- 基线提交：`2097a40e05ecc2d759e224aaa6588dc6a8667e98`
- 结果提交：以最终回复提供的单一提交 SHA 为准
- 允许修改范围：`website/components/AdminIncentives.tsx`、`website/app/globals.css`（仅后台样式）、`website/app/api/incentives/admin/previews/route.ts`、`website/lib/incentive-store.ts`、`website/esa/api.js`、ESA API 测试、`docs/api/WEBSITE_API.md` 与本交接记录

## 已完成

- “新功能内容”后台改为紧凑分层编辑：全局重点独立卡片；版本父级工具栏保留；当前版本功能条目显示摘要卡，默认收起；一次只展开一个模块；模块编辑默认简中，英文、繁中、日文通过语言标签按需切换；已有“保存并同步前台”作为明确提交操作。
- 新增 `DELETE /api/incentives/admin/previews`：请求体 `{ "id": "<preview-id>" }` 或查询参数 `?id=`，必须通过同源校验和管理员会话；成功返回被删除预告，找不到返回 404，内部功能内容记录拒绝删除。
- 后台编辑页和预告列表均提供删除入口；二次确认显示版本号、立即从前台移除的影响，并明确不会删除同版本新功能内容。删除当前编辑项后清空编辑草稿，失败保留原状态并显示错误。
- ESA 镜像、存储层、接口文档和测试同步更新；测试覆盖未认证拒绝、已发布预告删除、草稿删除、内部功能内容保护及预告删除不影响功能内容。

## 未修改 / 非目标

- 未修改公开前台视觉、`/api/features` 或公开预告响应契约。
- 未做数据库 schema 变更或真实数据迁移；删除是管理员明确确认后的真实记录删除，代码回滚不能恢复已删除记录。
- 未触发 ESA 发布，未修改桌面端、资产、密钥、环境变量或真实用户数据。

## 验证

- 命令：`npm run typecheck`
- 结果：通过。
- 命令：`npm run test:esa-api`
- 结果：通过。
- 命令：`npm run build:esa`
- 结果：通过。

## 风险与后续

- 已知限制：删除当前版本预告后，后台列表会移除该记录；若删除后数据库没有任何预告，既有存储兼容逻辑仍可能在下一次读取时创建默认预告。
- 交接目标：`05-quality-integration`；独立复核紧凑编辑器的单模块展开、四语切换不丢值、排序/显示/保存，以及已发布/草稿删除确认和同版本功能内容独立性。
- 回滚点：代码可回滚至基线提交；已执行的预告删除需依赖数据库备份或人工重建，不能依靠代码回滚恢复。
