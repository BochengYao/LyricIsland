# HANDOFF：官网后台移动端审计修复

- 日期：2026-08-16
- 基线：`3b0a51b8e5c236398e3faa3c5fb3806996ee5a29`
- 工作树：`D:\AppleMusicDesktopLyrics\.worktrees\web-admin-mobile-audit-3b0a51b`
- 初始审计提交：`fb75f7d`（五视口截图与修复前问题清单）
- 修复范围：`website/app/globals.css` 的管理后台移动断点；未修改 API、数据模型、公开前台视觉、桌面端或 ESA 发布环境。

## 已修复

- 320–767px 后台侧栏改为两列并让导航按钮填充列宽，避免中文标签与状态徽标挤出视口。
- 新功能内容的顶部操作、重点卡片头部和条目头部在移动端纵向堆叠；条目操作改为两列、全宽，保留版本内排序语义。
- 版本预告编辑操作和已发布预告操作在移动端改为单列全宽，避免长中文按钮被压缩。
- 移动断点提高新功能操作、审阅按钮和四语标签的最小高度至 44px，改善触控面积。

## 验证

- `npm run typecheck`：通过。
- `npm run test:esa-api`：通过（`ESA API tests passed`）。
- `npm run build:esa`：通过；静态路由生成完成，`website/app/api` 已由构建脚本正常恢复。
- 本地无登录截图：`docs/coordination/evidence/mobile-admin-audit-2026-08-16/` 下 320×568、360×800、390×844、414×896 竖屏及 844×390 横屏登录态截图。

## 真实性边界与后续复核

- 没有管理员会话，未输入密码，也没有执行任何管理写请求；登录后反馈、新功能、版本预告、访问日志、模态框、键盘焦点和真实长文本的视觉仍需质量线程在授权会话下复核。
- 初始截图证明登录页在五个目标视口可加载，不等同于生产部署验证。
- 共享工作树 `web-mobile-audit` 曾被前台移动审计占用，本任务已停止使用并迁移到本隔离树；不得把共享树的 WIP 或生成物带入本候选。
- 无数据迁移、无接口契约变化、无 ESA 触发；请交给 `05-quality-integration` 做授权会话回归后再由发布线程处理。
