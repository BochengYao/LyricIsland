# 任务交接：版本预告粗略上线范围与自动递减

- 日期：2026-09-05
- 任务线程：官网前台 + 官网后台与数据
- 基线提交：`1b0d2355c684330dae83f0511c0e5725d137322b`
- 结果提交：以包含本交接文件的提交 SHA 为准
- 允许修改范围：版本预告公开展示、管理端编辑控件、共享日期换算工具、后台样式、官网 API 契约与本交接记录

## 已完成

- 管理端“预计上线时间”从精确日期输入改为“今天”“明天”“本周内”“两周内”“本月内”“自定义天数”“待定”选择；自定义天数限制为 2–365 天。
- 继续使用现有 `target_date` 数据库列和 API 字段，不新增数据库迁移。管理端保存时把范围换算为目标日期，现有记录可继续读取和编辑。
- 官网更新页、激励页、后台已发布列表及草稿菜单统一显示粗略时间，不再公开展示精确日期。
- 粗略时间按访问当天动态计算；两周目标在设置当天显示“两周内”，次日显示“13天内”。已过期、空值或非法值回退为“待定”。
- 简中、繁中、日文、英文均提供对应时间文案。
- 恢复远端基线中三处已实现但被后续集成遗漏的官网代码：版本预告删除存储函数、四语功能内容类型/读取、`versions` 清洗回填；这些遗漏会直接阻断当前 ESA 静态构建。

## 未修改 / 非目标

- 未新增或修改 Supabase 列、RLS、管理员鉴权、版本预告正文结构或发布状态语义。
- 未修改桌面端、Microsoft Store 产物或用户未跟踪文件。

## 验证

- 纯函数定点验证：2026-09-05 保存“两周内”得到 `2026-09-19`；当天显示“两周内”，2026-09-06 显示“13天内”，英文显示 `Within 13 days`。
- 类型检查：通过（直接运行锁定依赖的 TypeScript CLI，退出码 0）。
- `npm run test:esa-api` 的等价直接脚本：通过，输出 `ESA API tests passed`。
- `npm run build:esa` 的等价直接脚本：通过；Next.js 16.2.10 完成 TypeScript 检查并静态生成 16 个产品路由（加 `_not-found` 共 17 页）。

## 风险与后续

- 已知限制：范围由访问设备的本地日历日期计算；跨时区用户在各自午夜切换文案，这是面向用户的预期行为。
- 交接目标：质量/发布复核后同步 GitHub 与 ESA，并验证真实生产更新页和后台控件。
- 回滚点：`1b0d2355c684330dae83f0511c0e5725d137322b`。
