# v3.2.49 MSIX Partner Center 修正交接

## 原因与修正

- Partner Center 拒绝 `3.2.48.1`：应用清单第四段版本号必须为 `0`。
- 单一版本源由 `3.2.48` 提升为 `3.2.49`，程序集文件版本与 MSIX 清单版本均为 `3.2.49.0`。
- `store/msix/build-msix.ps1` 恢复固定映射 `$productVersion.0`，移除第四段自动递增与 `PackageRevision` 参数，避免再次生成 Partner Center 不接受的包。
- MSIX 继续只声明 `zh-Hans`、`zh-Hant`、`en`、`ja`，不声明 `zh-CN`。

## 本地产物与验证

- `publish/current`：`3.2.49-Beta`，文件版本 `3.2.49.0`，11 个文件、33,834,334 bytes；当前候选已重新启动。
- MSIX：`store/package/msix/LyricHover_3.2.49.0_x64.msix`，87,988,861 bytes。
- SHA-256：`B117DE373418A7692A5DA7914584CC6172844D305AECA156286AF0851C31C15F`。
- 包身份：`70643607.LyricIsland`；架构：`x64`；签名状态：`NotSigned`。
- MakeAppx 封包与回解验证成功；直接读取包内清单确认版本、身份、主程序和四项语言正确，临时目录均已清理。
- 桌面回归：270 PASS / 0 FAIL；Release 构建：0 error。
- 已删除 Partner Center 拒绝的本地 `LyricHover_3.2.48.1_x64.msix`，避免误传。

## 外部状态

- 本交接记录的是本地候选生成与 Git/GitHub 源码交付；没有代替用户重新上传 Partner Center，也没有执行 Store 提交审核或发布。
