# 更新日志

本文件记录面向用户的已发布版本变化，遵循“先验证产物与实际发布状态，再写入更新日志”。

## [Unreleased]

## [3.2.40] - 2026-09-10

- 修复少数歌曲的逐字歌词数据只覆盖开头片段时，播放到中途停留在同一句的问题；残缺逐字歌词会被拒绝，并自动回退到完整逐行歌词。

## [3.2.38] - 2026-09-09

- 修复部分歌曲的逐字歌词与翻译包因内容分级后缀被误判为错误歌曲的问题，同时继续拒绝现场版、混音版等不同版本。
- 修复零起点逐字时间、短句翻译和翻译回退异常导致逐字追踪或翻译丢失的问题；缺失译文不会再被重复贴到相邻歌词。
- 修复 Widgets 恢复暂时失败时歌词坞被整体阻断的问题；存在安全空位时仍可显示，且不会覆盖恢复记录或自动修改新的系统设置。
- 修复歌词坞在任务栏空间由不可用恢复为可用后不能自动重显的问题。

## [3.2.37] - 2026-09-08

- 修复歌词坞开启后重启可能恢复为关闭的问题。系统恢复暂时失败时会保留你的开关选择，并在环境恢复后重试。
- 修复退出时歌词坞恢复流程的竞态，确保隐藏歌词坞窗口不会阻止应用正常退出。
- 修复混合格式歌词可能漏行，以及无法可靠判定的逐字时间错误高亮问题。
- 改进歌词缓存和在线歌词回退，避免无效结果覆盖可用歌词。
- 官网公开列表、点赞和兑换码操作现在会在并发与公开状态边界下保持一致。

- 移除酷狗在线歌词来源；酷狗播放器的 Windows SMTC 识别保持不变，旧设置中的酷狗歌词源偏好会安全回退到仍受支持的来源。
- 官网不再公开列举在线歌词提供方或技术端点，统一说明为“在线歌词由第三方来源按需获取”。

## [3.1.36]

- 支持页改为更易扫描的 2×2 系统设置分组，并重做关于页的字体层级与产品信息布局，移除过期的 v2.0 更新文案。

## [3.1.35]

- 设置界面所有模块统一使用四边 24px 安全距离，移除左右与上下不一致以及歌词模块单独放大的内边距。

## [3.1.34]

- 设置页滚动条改为自动淡入淡出的细浮层样式，压缩卡片纵向留白，并提升侧栏文字辨识度；导航按常用程度重排为“通用、歌词外观、模块布局、位置与状态、鼠标避让、快捷键、关于”。

## [3.1.33]

- 设置窗口外层与内部大卡片统一使用 Windows 11 顶层窗口的 8px 圆角，并移除 DWM 绘制的最外圈边框；下拉框、开关与其他胶囊控件继续保持与自身高度匹配的圆角。

## [3.1.32]

- 修复偏好设置中的圆角矩形被渲染成椭圆的问题：按钮、侧边栏选中态和关联弹窗统一使用同一套圆角层级，Toggle 与预览胶囊改用与高度匹配的明确半径。

## [3.1.31]

- 修复歌词坞设置两处状态问题：歌词坞正在显示时打开“歌词外观”页开关却显示关闭；设为左对齐后关闭再打开该页有时回退为居中对齐。
- 修复“鼠标避让”等页面可能无法显示的问题：页面可见性改为由外层容器直接控制，不再依赖嵌套可见性绑定。
- 偏好设置“通用”页拆为三张独立卡片：首选歌词源与播放器、节能模式、缓存容量各自成卡，不再共用同一个圆角模块。
- 偏好设置页面结构统一：各页主标题移至模块外（卡片内不再有标题），“通用”页合并为单张卡片并移除“缓存”标题，节能模式说明新增“查看详情”，展开可查看具体节能项（定时任务降频与过渡动画关闭）。
- 偏好设置信息架构重构：播放器选择迁移至“通用”页且说明紧贴选择框，模块布局页仅保留布局相关设置；滚动条改为独立于卡片的悬浮细条（6px、鼠标悬停 200ms 淡入、离开淡出），“自动折叠”布局卡片说明完整展示，设置行标签列宽统一。
- 修复偏好设置打开时崩溃的问题（开关动画遇到系统共享的冻结变换对象所致）。
- 偏好设置交互细节优化：设置内容支持鼠标滚轮滚动（滚动条隐藏），“歌词岛”“歌词坞”设置行对齐修复不再重叠，开关动画更顺滑且在系统关闭动画时直接切换到位，内容边缘改为渐变淡出。
- 偏好设置卡片视觉更贴近 macOS 系统设置：去除卡片描边改为明暗层次与极弱阴影，“通用”页内容铺满右侧区域，缓存卡片更紧凑。
- 偏好设置细节打磨：设置卡片不再响应鼠标悬停，“歌词外观”拆分为“歌词岛”“歌词坞”两张独立卡片，“模块布局”的播放器说明完整展示，各页卡片内边距与标题间距统一，缓存“查看详情”可正常展开收起。
- 偏好设置进一步贴近 macOS 原生观感：卡片悬停改为亮度微提与阴影加深（移除边缘光晕），侧边栏更轻盈，开关采用 System Blue 与更大圆点，文字层级更分明。
- 偏好设置整体升级为 Apple 风格：半透明玻璃卡片、深黑灰/浅色新调色板、更轻盈的侧边栏与页面切换淡入动画。
- 偏好设置现在自动即时生效，底部仅保留胶囊形“完成”按钮（原“保存/应用/取消”已移除）。
- “通用”页拆分为两张卡片：省电模式更名“节能模式”；缓存说明简化为一句话，可点“查看详情”展开完整规则。
- 偏好设置左侧“歌词显示”更名为“通用”，新增“歌词外观”页面，分为“歌词岛”与“歌词坞”两个模块，可用开关分别开启或关闭，模块间距更清晰。
- 歌词岛与歌词坞各自拥有独立的行数与翻译显示设置（歌词坞另有对齐方式滑动选择），互不影响；翻译开启时点击单行的提示会出现在对应模块旁。
- 偏好设置“通用”页新增缓存设置（含容量说明），省电模式改为开关样式，原“缓存”导航项已移除。
- 歌词译文会按当前原词对齐，容忍小幅时间戳偏差且不会把上一句译文复用到下一句。
- 网易云音乐与 QQ 音乐在本地化曲名未命中时，可在严格的歌手、时长与版本冲突校验后恢复匹配原始曲名歌词。

## [2.0.36]

- 历史版本基线；具体用户可见变更以已验证的 Microsoft Store 发布记录为准。

## 维护规则

- 发布线程负责新增版本条目；功能线程不得自行把未发布功能写成已发布。
- 每项条目写用户可感知的变化、兼容性提示和必要的迁移说明，不写内部重构细节。
