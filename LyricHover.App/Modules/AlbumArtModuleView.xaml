<UserControl x:Class="LyricHover.App.Modules.AlbumArtModuleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="60"
             Height="48">
    <UserControl.Resources>
        <LinearGradientBrush x:Key="NeutralArtworkBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#303030" Offset="0" />
            <GradientStop Color="#141414" Offset="1" />
        </LinearGradientBrush>
    </UserControl.Resources>
    <Border x:Name="ArtworkFrame"
            Width="42"
            Height="42"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            CornerRadius="4"
            Background="{StaticResource NeutralArtworkBrush}"
            ClipToBounds="True">
        <Image x:Name="ArtworkImage"
               Stretch="UniformToFill"
               SnapsToDevicePixels="True">
            <Image.Clip>
                <RectangleGeometry Rect="0,0,42,42" RadiusX="4" RadiusY="4" />
            </Image.Clip>
        </Image>
    </Border>
</UserControl>
